startsWith() is a built-in method you call on strings.It checks whether the string starts with specified word,path etc.This method takes two parameters.
This method will return true with the path is correct and false if the path is incorrect
          
          SYNTAX:
                   string.startsWith(searchString,postion)
               SearchString : stores the string needed to search.
               Position     : where the string is present
